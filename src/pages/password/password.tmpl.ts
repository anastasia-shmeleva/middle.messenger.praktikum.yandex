export default `
  {{{ sideBtn }}}
  <div class="profile">
    <div class="profile-header">
      {{{ avatar }}}
    </div>
    <div class="profile-body">
      <form id="password" class="profile-form">
        {{{ oldPassword }}}
        {{{ newPassword }}}
        {{{ confirmPassword }}}
      </form>
    </div>
    <div class="profile-footer">
      {{{ button }}}
    </div>
  </div>
`
