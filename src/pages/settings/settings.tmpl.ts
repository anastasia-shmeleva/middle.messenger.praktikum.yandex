export default `
  {{{ sideBtn }}}
  <div class="profile">
    <div class="profile-header">
      {{{ avatar }}}
    </div>
    <div class="profile-body">
      <form id="user" class="profile-form">
        {{{ email }}}
        {{{ login }}}
        {{{ firstName }}}
        {{{ secondName }}}
        {{{ displayName }}}
        {{{ phone }}}
      </form>
    </div>
    <div class="profile-footer">
      {{{ button }}}
    </div>
  </div>
`
